# 智能校园综合服务平台

**独立开发 | 2026.06 - 2026.07 | Spring Boot · Spring AI Alibaba · MyBatis-Plus · Vue3**

**[github.com/lan-hash/Smart-Campus](https://github.com/lan-hash/Smart-Campus)**

基于 Spring Boot + Spring AI Alibaba 的 AI 驱动校园综合服务平台，涵盖用户管理、校园论坛、二手交易、AI 助手等 9 大业务模块，独立完成 87+ API 接口、27 张数据库表及完整 Vue3 前端。

- **AI 智能模块**：基于 Spring AI Alibaba 接入通义大模型，落地 AI 对话（UUID 管理多轮会话）、内容审核（结构化 prompt + pass/block/review 三档风险判定）、帖子智能分类（动态构建版块列表 prompt）。集成时 M2 版本 `ChatClient.prompt()` 不支持传入 String，升级至 M5.1 解决 API 兼容性问题；

- **二手交易模块**：交易涉及商品和交易两张表状态同步，存在并发覆盖风险。设计双重状态机（商品：在售→预留→已售；交易：待确认→进行中→完成/取消），通过 `LambdaUpdateWrapper.eq(status, 1)` 乐观锁实现状态联动更新，保证并发场景下数据一致性。购买流程设置 4 道校验覆盖完整业务闭环；

- **论坛社区模块**：评论树加载存在 N+1 查询问题（N 条顶级评论需 2N+1 次），改为批量查询 + `Stream.groupingBy` 组装树结构，固定 3 次查询完成。点赞/收藏用 toggle 模式保证幂等性，避免重复操作产生脏数据。
